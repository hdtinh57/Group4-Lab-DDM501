global:
  scrape_interval: 10s
  evaluation_interval: 10s
rule_files:
  - alerts/*.yml
scrape_configs:
  - job_name: prometheus
    static_configs: [{targets: ["localhost:9090"]}]
  - job_name: movie-rating-api
    metrics_path: /metrics
    static_configs: [{targets: ["api:8000"]}]
